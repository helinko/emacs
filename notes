Things to find out about emacs:

What to do with the title bar?

Are my ido settings reasonable? How to
add a file when the new name matches with an old one?

Some way to deal with too long lines. The line length probably has to
be dependent of the machine I'm using.

Look into hippie-expand

Highlight current line (was unusable with the current theme). Settiung in editing.el.

Backup files. These are turned on in editing.el but there are still ~
files.

Commenting regions.

The way these settings are divided into files doesn't make much sense.

'Import' colors from regular shell to M-x shell.